# big O notation 
- it is a label that tell how fast the algorithm is 
- O(n), O(n**2), O(log(n))
## O(1) 
- work is only one regardless of the data size
- work fo code is independent to the data size( how many items you have) 
- ex: you are picking arr[0] from a list
## O(log(n))
- you are eleminating the half possibility where there is no chance of getting item 
- work of code is logarithmic to the data size
- ex: binary search
## O(n)
- loop is executed once 
## O(n**2)
- work of code is directly proportional to the data size square
- loop inside loop fo finding the dupplicates
## O(2**n)
- worst 